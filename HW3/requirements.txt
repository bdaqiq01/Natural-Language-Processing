# Assignment 3 — NLP HW
# Q1: gensim (GloVe downloader / KeyedVectors). Q2: scikit-learn.
# ipykernel: run .ipynb in Cursor/VS Code/Jupyter without the heavy "jupyter" meta-package
# (avoids Windows MAX_PATH failures from long jupyterlab widget paths).
gensim>=4.3.0
scikit-learn>=1.3.0
ipykernel>=6.25.0
